spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    show-sql: false

server:
  error:
    include-message: never
    include-binding-errors: never

logging:
  level:
    root: WARN
    com.skyfreight: INFO
